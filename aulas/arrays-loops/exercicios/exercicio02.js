const array = [12, 34, 'Pâmela'];

for (let i = 0; i < array.length; i++) {
    const element = array[i];
    console.log(element);
}
console.log('-----------------------');

let indice = 0;
while(indice < array.length){
    console.log(array[indice]);
    indice++;
}