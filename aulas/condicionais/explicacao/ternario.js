// Serve para simplificar condições simples para uma linha.

const precoIngresso = 12;
const unidadeIngresso = (precoIngresso == 1 ? "real" : "reais");

console.log(`O preço do ingresso é ${precoIngresso} ${unidadeIngresso}.`);