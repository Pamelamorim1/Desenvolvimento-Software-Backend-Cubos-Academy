// Velocidade Média

let distanciaPercorridaEmMetros = 500;
let tempoEmSegundos = 10;
let velocidadeMedia = (distanciaPercorridaEmMetros / tempoEmSegundos).toFixed(0);

console.log(`A velocidade percorrida foi de ${velocidadeMedia * 3.6}km/h`);
