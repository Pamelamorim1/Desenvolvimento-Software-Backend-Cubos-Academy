// Calcular Área total de um cilindro

const pi = Math.PI;
let alturaCilindro = 1; 
let raioBase = 3;
const areaTotal = 2 * pi * raioBase * (raioBase + alturaCilindro);

console.log(`Área do cilindro com altura ${alturaCilindro} e raio o ${raioBase} é igual a ${areaTotal}`);
